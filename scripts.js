const gameInfo = {};

gameInfo.title = prompt("What is the title of your game?");
if ((gameInfo.title = "Halo Reach")) {
  gameInfo.map = "Zealot, Countdown, Forge World";
}
gameInfo.map = prompt("Which map are you playing?");
if ((gameInfo.map = "Zealot")) {
  gameInfo.mode = "Capture the Flat, Team Slayer";
}
